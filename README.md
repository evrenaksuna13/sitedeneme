
  # GAF Website Replica (Copy)

  This is a code bundle for GAF Website Replica (Copy). The original project is available at https://www.figma.com/design/uHRhIKZqV8HwwQorZjMlbz/GAF-Website-Replica--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  